import 'package:flutter/material.dart';

class FilesAssetsScreen extends StatelessWidget {
  const FilesAssetsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Files & Assets')),
      body: const Center(child: Text('Files & Assets Screen')),
    );
  }
}
